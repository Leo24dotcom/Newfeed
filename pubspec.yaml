name: mypage
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  country_flags: ^4.1.2

flutter:
  uses-material-design: true
  assets:
    - assets/images/pfp.jpeg